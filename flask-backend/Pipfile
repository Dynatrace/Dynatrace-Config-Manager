[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = ">=2.31.0"
flask = ">=3.0.0"
flask-cors = ">=4.0.0"
autodynatrace = ">=2.0.0"
deepdiff = ">=6.7.1"
colorama = ">=0.4.6"
cx-freeze = ">=6.15.12"
charset-normalizer = ">=3.3.2"
pyyaml = ">=6.0.1"
urllib3 = ">=2.1.0"
jinja2 = ">=3.1.3"

[dev-packages]

[requires]
python_version = "3.9"
